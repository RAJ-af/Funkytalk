package com.itsraj.funkytalk.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Warm Neutral Palette ─────────────────────────────────
val FunkyBackground = Color(0xFFFFFDF8)        // Warm off-white screen bg
val FunkySurface = Color(0xFFFFFFFF)            // Card/surface white
val FunkySurfaceElevated = Color(0xFFF7F6F2)    // Tab bg, input bg, subtle surfaces
val FunkyNavBg = Color(0xFFFFFFFF)              // Floating nav background

// ─── Text ──────────────────────────────────────────────────
val FunkyTextPrimary = Color(0xFF1A1A1A)         // Headlines, body
val FunkyTextSecondary = Color(0xFF9E9E9E)       // Subtitles, captions
val FunkyTextTertiary = Color(0xFFC5C5C5)        // Placeholders, disabled
val FunkyTextOnYellow = Color(0xFF1A1A1A)        // Text on yellow bg

// ─── Accent ────────────────────────────────────────────────
val FunkyYellow = Color(0xFFF6C945)              // Primary accent
val FunkyYellowSoft = Color(0xFFFFF4D6)          // Soft yellow bg for pills
val FunkyYellowLight = Color(0xFFFFF9E6)         // Very light yellow bg

// ─── Borders ───────────────────────────────────────────────
val FunkyBorder = Color(0xFFEBEBEB)              // Standard borders
val FunkyBorderLight = Color(0xFFF0EFED)         // Lighter borders, dividers

// ─── States ────────────────────────────────────────────────
val FunkyError = Color(0xFFE57373)               // Error text, destructive
val FunkySuccess = Color(0xFF4CAF50)             // Online indicator, success
val FunkyActiveYellow = Color(0xFFF6C945)        // Active tab, selected state
val FunkyInactiveGray = Color(0xFFD4D4D4)        // Inactive tab, unselected

// ─── Shadows (use with Modifier.shadow) ────────────────────
val FunkyShadowAmbient = Color(0x0F000000)       // ~6% black
val FunkyShadowSpot = Color(0x14000000)           // ~8% black
